<resources>

    <style name="AppTheme.Transparent" parent="AppTheme">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="StartStyle" parent="AppTheme">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@drawable/start</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>


    <!-- Picture Activity theme -->
    <style name="AppTheme.Picture" parent="AppTheme.Transparent">
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <!-- Tab title -->
    <style name="GankTabText" parent="@android:style/TextAppearance">
        <item name="android:textSize">18sp</item>
    </style>

    <!-- CollapsingToolbar title -->
    <style name="CollapsingToolbarTitle" parent="@android:style/TextAppearance">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
